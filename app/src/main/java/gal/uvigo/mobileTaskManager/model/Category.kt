package gal.uvigo.mobileTaskManager.model

/**
 * Enum class to represent a Task Category
 */
enum class Category {
    OTHER, WORK, PERSONAL, URGENT
    //There is a String-array resource associated, order MUST be maintained
}